## The order of the presentation in the last two weeks is randomly assigned
## with a random seed determined by all participants.

presenters <- c("Chiovaro", "Fu", "Lau", "Jiao", "Palmer", "Qi", "Xu")

set.seed(7026533)  # each presenter contributed one digit

sample(presenters)

